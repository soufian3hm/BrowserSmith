{
  "name": "buildgpt",
  "version": "0.1.0",
  "description": "Four-tab ChatGPT agent loop that plans, writes, reviews and audits real projects, then runs them",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "mcp": "node src/mcp/server.js",
    "test": "node --test test/protocol.test.js"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.4",
    "electron": "^33.2.0",
    "png-to-ico": "^3.0.2",
    "sharp": "^0.35.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
